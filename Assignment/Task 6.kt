fun main() {
    // Create an array of 5 names
    val names = arrayOf("Faruk", "Muhammad", "Sadik", "Playboy", "Aisha")

    // Loop through the array and print each name with a greeting
    for (name in names) {
        // Print a greeting message with the name
        println("Hello, $name!")
    }
}